import type { InvitationData } from '../types/planet.types'

export const INVITATION: InvitationData = {
    title: 'RSVP',
    subtitle: 'Please confirm your attendance so we can prepare for your arrival.',
}
